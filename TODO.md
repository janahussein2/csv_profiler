
- intiialize the repository
- add the code files
- push them to the github project


- support command line using typer
- streamlit show the report as a table in the UI, not the CSV itself
- add support for uvx command to work from github
